apiVersion: v1
kind: Service
metadata:
  name: {{ include "ssl-expire.fullname" . }}
  labels:
    {{- include "ssl-expire.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "ssl-expire.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP